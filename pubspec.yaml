name: crypto_converter
description: Crypto currency converter

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

  bloc: ^8.1.1
  flutter_bloc: ^8.1.2
  get_it: ^7.2.0
  http: ^0.13.5
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  mockito: ^5.3.2
  build_runner: ^2.3.3

flutter:
  uses-material-design: true